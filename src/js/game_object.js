// Generated by CoffeeScript 1.7.1
(function() {
  var GameObject, Pulser;

  window.Pulser = Pulser = (function() {
    function Pulser() {}

    return Pulser;

  })();

  window.GameObject = GameObject = (function() {
    GameObject.showHitboxes = true;

    GameObject.prototype.image = null;

    function GameObject(position, size, velocity, rotation, scale) {
      this.position = position;
      this.size = size;
      this.velocity = velocity != null ? velocity : $V([0, 0]);
      this.rotation = rotation != null ? rotation : 0;
      this.scale = scale != null ? scale : 1;
      this.image = Img.frankie;
      this.wave = new Wave(0);
    }

    GameObject.prototype.update = function() {
      this.wave.update();
      return this.position = this.position.add(this.velocity);
    };

    GameObject.prototype.center = function() {
      return this.position.add(this.centerOffset());
    };

    GameObject.prototype.centerOffset = function() {
      return this.size.x(1 / 2);
    };

    GameObject.prototype.draw = function(ctx) {
      var center, scale;
      center = this.center();
      ctx.translate(center.e(1), center.e(2));
      if (GameObject.showHitboxes) {
        ctx.fillStyle = 'rgb(200,0,0)';
        ctx.beginPath();
        ctx.arc(0, 0, this.size.e(1) / 2, 0, Math.PI * 2, true);
        ctx.closePath();
        ctx.fill();
      }
      ctx.rotate(this.rotation + this.wave.value() * 0.2);
      scale = this.scale + this.wave.value() * 0.1;
      ctx.scale(scale, scale);
      if (this.image != null) {
        return ctx.drawImage(this.image, -this.size.e(1) / 2, -this.size.e(2) / 2, this.size.e(1), this.size.e(2));
      }
    };

    return GameObject;

  })();

}).call(this);
